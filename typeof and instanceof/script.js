// typeof ✅
// It uses to know the value's type for example:
// typeof 5 = Number;
// typeof "5" = String;
// but it tells the NaN's value type as a Number and an Arrays's value type as an object which are it's quirks

// instanceof ✅
// It uses to know that does the value belong to the given value-type or not Example:

// let a = [];
// a instanceof Array // true, herr because a is belong to an array.

// let a = {}

// a instanceof Array //false, because here a belongs to an object. Not an Array.


